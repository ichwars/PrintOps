
import { Checkbox } from '../../ui';

export function FollowUpActions({ locale }: { locale: string }) { const de = locale.startsWith('de'); return <section className="rounded-xl border border-bambu-dark-tertiary bg-bambu-dark p-4"><h3 className="font-semibold text-white">{de ? 'Optionale Folgeaktionen' : 'Optional follow-up actions'}</h3><div className="mt-3 flex flex-wrap gap-6 text-sm text-bambu-gray"><label className="flex items-center gap-2"><Checkbox checked={false} disabled />{de ? 'Angebotsentwurf erstellen' : 'Create offer draft'}</label><label className="flex items-center gap-2"><Checkbox checked={false} disabled />{de ? 'Als Druckauftrag übernehmen' : 'Create print order'}</label></div><p className="mt-2 text-xs text-bambu-gray">{de ? 'Wird mit den Angebots- und Auftragsworkflows aktiviert.' : 'Will be enabled with the offer and order workflows.'}</p></section>; }
